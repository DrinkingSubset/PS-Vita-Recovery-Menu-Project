cmake_minimum_required(VERSION 3.5)
project(BootTriggerUser)

include("${VITASDK}/share/vita.cmake" REQUIRED)

set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wl,-q -Wall -O2")

add_executable(boot_trigger_user
    boot_trigger_user.c
)

target_link_libraries(boot_trigger_user
    SceAppMgr_stub
    SceIofilemgr_stub
    SceKernelThreadMgr_stub
    SceLibKernel_stub
)

vita_create_self(boot_trigger.suprx boot_trigger_user
    UNSAFE
)
