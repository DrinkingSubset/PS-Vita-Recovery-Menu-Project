#pragma once
/*
 * recovery_installer.h — R-Trigger Boot Recovery Installer
 *
 * Installs / uninstalls the boot_recovery.skprx kernel plugin.
 * Accessed from Advanced -> Boot Recovery Installer.
 */

void recovery_installer_run(void);
